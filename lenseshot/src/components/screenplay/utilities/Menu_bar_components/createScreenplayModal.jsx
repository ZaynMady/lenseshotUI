import axios, { AxiosHeaders } from 'axios'

export default function createScreenplayModal({scriptData, scriptName, scriptLocation}){
    //creating a screenplay in project
    const handleCreateinProject = async (scriptData, ProjectID, scriptName) => {
        //getting Authorization header

        //building screenplayJson which exists of Node class name : content


        //creating a screenplay first
        const response = await axios.put(
            "screenplayurl",
            
            
            
        )

        //
    }

    //creating a personal screenplay
    const handleCreatePersonal = async (scriptData,scriptName, scriptLocation ) => {


    }
    return(
        <div>

        </div>
    )
}